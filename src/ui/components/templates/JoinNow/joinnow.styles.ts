import styled from "styled-components";
import { Container } from "reactstrap";

const Join = styled(Container)`
    background: #FAFAF9;
    padding-left: 0;
    padding-right: 0;
`;

export default Join;
